# 毛玻璃（云母）风格设计规范 v0.1

适用项目：纸机驿站 / PaperPlane Relay  
设计关键词：轻盈、半透明、柔和、层级、漂浮、安静、现代、梦幻

---

## 1. 风格定位

本项目采用 **云母 Mica 背景 + 毛玻璃 Glass 卡片** 的视觉风格。

整体效果应像：

> 一架纸飞机停在安静的云层玻璃上。

这个风格适合匿名随机内容交换网站，可以营造温柔、神秘、轻社交、低压力表达的氛围。

---

## 2. 设计目标

页面需要做到：

- 轻盈，不厚重
- 通透，但内容清晰
- 柔和，但按钮可点
- 背景有氛围，但不抢正文
- 组件有漂浮感，但不杂乱
- 浅色模式和深色模式都能使用
- 移动端性能稳定

核心原则：

```text
云母做底，玻璃做层，柔光做气氛，文字保持清晰。
```

---

## 3. 毛玻璃与云母的使用区别

## 3.1 云母 Mica

云母适合作为大面积底层背景。

特点：

- 低对比
- 轻微透明感
- 柔和色彩
- 稳定、不花
- 不强调强实时模糊

使用位置：

- 页面主背景
- 主容器底层
- 侧边栏
- 后台页面底色

---

## 3.2 毛玻璃 Glass / Acrylic

毛玻璃适合作为浮在背景上的内容层。

特点：

- 半透明
- 背景模糊
- 有边框高光
- 有柔和阴影
- 层级感明显

使用位置：

- 内容卡片
- 顶部导航
- 弹窗
- 输入框
- Toast 提示
- 纸飞机展示卡片

---

## 3.3 本项目推荐组合

```text
页面背景：云母 Mica
主要内容：毛玻璃卡片
弹窗浮层：强毛玻璃
按钮输入框：半透明柔光
后台管理：弱云母 + 清晰表格
```

---

## 4. 页面层级规范

从下到上分为 5 层：

```text
第 0 层：背景渐变 / 光斑
第 1 层：云母底板
第 2 层：普通毛玻璃卡片
第 3 层：弹窗 / 下拉菜单
第 4 层：Toast / 重要提示
```

层级越高：

- 背景越不透明
- 模糊越强
- 阴影越明显
- 边框越亮
- 圆角可以略大

---

## 5. 背景设计规范

## 5.1 推荐背景

推荐使用：

- 柔和渐变
- 大面积模糊光斑
- 蓝紫色云层感背景
- 低饱和天空色
- 轻微颗粒纹理

不推荐使用：

- 高对比照片
- 复杂街景
- 纯黑纯白背景
- 强彩虹渐变
- 高频噪点
- 大面积红色

---

## 5.2 背景 CSS 示例

```css
body {
  min-height: 100vh;
  margin: 0;
  background:
    radial-gradient(circle at 20% 10%, rgba(104, 216, 255, 0.32), transparent 32%),
    radial-gradient(circle at 80% 20%, rgba(177, 140, 255, 0.28), transparent 30%),
    radial-gradient(circle at 45% 90%, rgba(255, 154, 203, 0.18), transparent 34%),
    var(--bg-base);
}
```

---

## 5.3 轻微颗粒纹理

可以加很淡的噪点，让背景不那么塑料。

```css
.noise::before {
  content: "";
  position: fixed;
  inset: 0;
  pointer-events: none;
  opacity: 0.035;
  background-image: url("/noise.png");
  mix-blend-mode: overlay;
}
```

要求：

```text
透明度：2% ~ 5%
不能明显看到噪点
不能影响阅读
```

---

## 6. 颜色规范

## 6.1 主色

推荐主色为柔和蓝紫。

```css
--color-primary: #6C8CFF;
--color-primary-hover: #5D7CF0;
--color-primary-active: #4D6CE0;
--color-primary-soft: rgba(108, 140, 255, 0.16);
```

---

## 6.2 辅助色

```css
--color-cyan: #68D8FF;
--color-purple: #B18CFF;
--color-pink: #FF9ACB;
--color-green: #78E0B6;
--color-yellow: #FFE08A;
```

辅助色只用于：

- 纸飞机颜色
- 状态标签
- 小光斑
- 空状态插画
- 成功提示

不要大面积铺满。

---

## 6.3 浅色模式 Token

```css
:root {
  --bg-base: #EEF3FF;
  --bg-mica: rgba(245, 248, 255, 0.72);
  --bg-glass: rgba(255, 255, 255, 0.48);
  --bg-glass-strong: rgba(255, 255, 255, 0.72);

  --text-primary: #172033;
  --text-secondary: rgba(23, 32, 51, 0.72);
  --text-muted: rgba(23, 32, 51, 0.48);

  --border-glass: rgba(255, 255, 255, 0.58);
  --border-soft: rgba(80, 100, 140, 0.14);

  --shadow-glass: 0 16px 40px rgba(31, 45, 80, 0.14);
}
```

---

## 6.4 深色模式 Token

```css
[data-theme="dark"] {
  --bg-base: #0E1320;
  --bg-mica: rgba(18, 24, 38, 0.74);
  --bg-glass: rgba(28, 36, 56, 0.50);
  --bg-glass-strong: rgba(34, 44, 68, 0.76);

  --text-primary: #F2F6FF;
  --text-secondary: rgba(242, 246, 255, 0.72);
  --text-muted: rgba(242, 246, 255, 0.46);

  --border-glass: rgba(255, 255, 255, 0.16);
  --border-soft: rgba(255, 255, 255, 0.08);

  --shadow-glass: 0 20px 50px rgba(0, 0, 0, 0.34);
}
```

---

## 7. 毛玻璃卡片规范

## 7.1 普通玻璃卡片

```css
.glass-card {
  position: relative;
  background: var(--bg-glass);
  backdrop-filter: blur(22px) saturate(140%);
  -webkit-backdrop-filter: blur(22px) saturate(140%);
  border: 1px solid var(--border-glass);
  border-radius: 24px;
  box-shadow: var(--shadow-glass);
}
```

使用位置：

- 首页功能卡片
- 接收纸飞机卡片
- 收藏列表卡片
- 简单信息面板

---

## 7.2 强玻璃卡片

用于弹窗、确认框、举报窗口。

```css
.glass-card-strong {
  background: var(--bg-glass-strong);
  backdrop-filter: blur(30px) saturate(160%);
  -webkit-backdrop-filter: blur(30px) saturate(160%);
  border: 1px solid var(--border-glass);
  border-radius: 28px;
  box-shadow: 0 24px 70px rgba(0, 0, 0, 0.22);
}
```

---

## 7.3 卡片边框

玻璃卡片必须有轻微边框，否则会糊成一片。

浅色模式：

```css
border: 1px solid rgba(255, 255, 255, 0.58);
```

深色模式：

```css
border: 1px solid rgba(255, 255, 255, 0.16);
```

---

## 7.4 顶部高光

```css
.glass-card::before {
  content: "";
  position: absolute;
  inset: 0;
  border-radius: inherit;
  pointer-events: none;
  background: linear-gradient(
    180deg,
    rgba(255,255,255,0.34),
    transparent 38%
  );
}
```

注意：

```text
高光只能增强玻璃质感，不能盖住正文。
```

---

## 8. 圆角规范

```css
--radius-xs: 8px;
--radius-sm: 12px;
--radius-md: 16px;
--radius-lg: 24px;
--radius-xl: 32px;
--radius-full: 999px;
```

使用规则：

```text
小按钮：12px
胶囊按钮：999px
输入框：16px
普通卡片：24px
弹窗：28px
页面主容器：32px
头像 / 图标按钮：999px
```

不要一个页面里圆角乱跳。

---

## 9. 阴影规范

阴影要柔，不要脏。

```css
--shadow-sm: 0 6px 18px rgba(31, 45, 80, 0.10);
--shadow-md: 0 16px 40px rgba(31, 45, 80, 0.14);
--shadow-lg: 0 24px 70px rgba(31, 45, 80, 0.20);
```

深色模式：

```css
[data-theme="dark"] {
  --shadow-sm: 0 6px 18px rgba(0, 0, 0, 0.22);
  --shadow-md: 0 16px 40px rgba(0, 0, 0, 0.32);
  --shadow-lg: 0 24px 70px rgba(0, 0, 0, 0.44);
}
```

禁用：

```text
不要使用硬黑阴影
不要使用纯黑 50% 以上阴影
不要让阴影边缘过实
```

---

## 10. 排版规范

## 10.1 字体

中文推荐：

```text
阿里巴巴普惠体
思源黑体
HarmonyOS Sans SC
系统默认 sans-serif
```

CSS：

```css
font-family:
  "Alibaba PuHuiTi",
  "Source Han Sans SC",
  "HarmonyOS Sans SC",
  "Microsoft YaHei",
  system-ui,
  sans-serif;
```

---

## 10.2 字号

桌面端：

```text
页面大标题：40px / 48px
页面标题：32px / 40px
区块标题：24px / 32px
卡片标题：20px / 28px
正文：16px / 26px
辅助文字：14px / 22px
说明文字：12px / 18px
```

移动端：

```text
页面大标题：32px
页面标题：26px
区块标题：22px
正文：16px
辅助文字：14px
```

---

## 10.3 字重

```text
大标题：700
标题：600
按钮：600
正文：400
辅助文字：400
```

不要全站大量使用 900 字重。

---

## 11. 布局规范

## 11.1 页面宽度

桌面端：

```text
最大内容宽度：1180px
页面左右边距：32px
卡片间距：24px
主内容区域：12 栅格
```

平板端：

```text
页面左右边距：24px
卡片间距：20px
```

移动端：

```text
页面左右边距：16px
卡片间距：16px
单列布局
```

---

## 11.2 间距系统

使用 8px 间距系统。

```text
4px：极小间距
8px：图标与文字
12px：小组件内部
16px：普通组件内部
24px：卡片间距
32px：区块间距
48px：大区块间距
64px：页面模块间距
```

---

## 12. 组件规范

## 12.1 顶部导航栏

```css
.navbar {
  height: 64px;
  background: rgba(255, 255, 255, 0.36);
  backdrop-filter: blur(24px) saturate(150%);
  -webkit-backdrop-filter: blur(24px) saturate(150%);
  border-bottom: 1px solid rgba(255,255,255,0.32);
}
```

要求：

```text
桌面端可以 fixed
移动端建议 sticky
当前页面状态需要高亮
导航文字必须清晰
```

---

## 12.2 主按钮

```css
.button-primary {
  height: 44px;
  padding: 0 22px;
  border-radius: 999px;
  border: 1px solid rgba(255,255,255,0.42);
  background: linear-gradient(135deg, #6C8CFF, #B18CFF);
  color: white;
  font-weight: 600;
  box-shadow: 0 10px 24px rgba(108, 140, 255, 0.28);
  transition: transform 180ms ease-out, box-shadow 180ms ease-out;
}

.button-primary:hover {
  transform: translateY(-1px);
  box-shadow: 0 14px 32px rgba(108, 140, 255, 0.34);
}

.button-primary:active {
  transform: translateY(0) scale(0.98);
}
```

---

## 12.3 次按钮

```css
.button-secondary {
  height: 44px;
  padding: 0 22px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.34);
  border: 1px solid var(--border-glass);
  color: var(--text-primary);
  backdrop-filter: blur(18px);
}
```

---

## 12.4 输入框

```css
.input {
  min-height: 46px;
  padding: 0 16px;
  border-radius: 16px;
  background: rgba(255, 255, 255, 0.34);
  border: 1px solid var(--border-glass);
  color: var(--text-primary);
  outline: none;
}

.input:focus {
  border-color: rgba(108, 140, 255, 0.72);
  box-shadow: 0 0 0 4px rgba(108, 140, 255, 0.16);
}
```

---

## 12.5 文本域

用于写纸飞机内容。

```css
.textarea-plane {
  min-height: 180px;
  resize: vertical;
  padding: 18px;
  border-radius: 22px;
  background: rgba(255, 255, 255, 0.38);
  backdrop-filter: blur(18px);
  border: 1px solid var(--border-glass);
  line-height: 1.7;
}
```

要求：

```text
必须显示字数统计
超过字数时边框变红
输入文字不能被背景干扰
```

---

## 12.6 纸飞机展示卡片

```css
.plane-card {
  position: relative;
  padding: 32px;
  border-radius: 28px;
  background: var(--bg-glass);
  backdrop-filter: blur(26px) saturate(150%);
  -webkit-backdrop-filter: blur(26px) saturate(150%);
  border: 1px solid var(--border-glass);
  box-shadow: var(--shadow-glass);
}
```

内容结构：

```text
顶部：纸飞机图标 / 颜色标签
中间：正文内容
底部：时间 / 点赞 / 收藏 / 举报
```

---

## 12.7 弹窗

```css
.modal {
  width: min(520px, calc(100vw - 32px));
  padding: 28px;
  border-radius: 28px;
  background: var(--bg-glass-strong);
  backdrop-filter: blur(34px) saturate(160%);
  -webkit-backdrop-filter: blur(34px) saturate(160%);
  border: 1px solid var(--border-glass);
  box-shadow: var(--shadow-lg);
}

.modal-mask {
  background: rgba(8, 12, 20, 0.32);
  backdrop-filter: blur(8px);
}
```

---

## 13. 图标规范

推荐图标风格：

```text
线性图标
圆角端点
2px 描边
少量填充
主图标可使用渐变
```

纸飞机图标要求：

```text
主纸飞机：蓝紫渐变
普通操作：当前文字色 72% 透明度
危险操作：柔和红色
成功状态：柔和绿色
```

不推荐：

```text
厚重实心图标
复杂写实图标
金属拟物图标
```

---

## 14. 动效规范

## 14.1 动效原则

```text
持续时间：160ms ~ 320ms
缓动：ease-out / cubic-bezier(0.2, 0.8, 0.2, 1)
位移：2px ~ 12px
缩放：0.98 ~ 1.04
```

动效要轻，不要晃。

---

## 14.2 页面进入

```css
.page-enter {
  animation: pageIn 260ms ease-out both;
}

@keyframes pageIn {
  from {
    opacity: 0;
    transform: translateY(10px);
    filter: blur(6px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
    filter: blur(0);
  }
}
```

---

## 14.3 纸飞机投递动画

投递成功时：

```text
纸飞机从按钮附近飞出
向右上方移动
逐渐缩小
透明度降低
最后消失
```

```css
@keyframes planeSend {
  0% {
    opacity: 1;
    transform: translate(0, 0) rotate(0deg) scale(1);
  }
  70% {
    opacity: 1;
    transform: translate(120px, -90px) rotate(18deg) scale(0.86);
  }
  100% {
    opacity: 0;
    transform: translate(180px, -140px) rotate(24deg) scale(0.7);
  }
}
```

---

## 14.4 接收纸飞机动画

接收时：

```text
远处小纸飞机飞入
落到卡片中心
卡片展开
文字淡入
```

不建议：

```text
太快
抖动
旋转过猛
音效过大
```

---

## 14.5 减少动效模式

必须适配系统减少动效设置。

```css
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.001ms !important;
    transition-duration: 0.001ms !important;
    scroll-behavior: auto !important;
  }
}
```

---

## 15. 响应式规范

## 15.1 桌面端

```text
首页可以左右分栏
左侧文案
右侧纸飞机预览卡片
导航固定顶部
主要卡片宽度 520px ~ 720px
```

---

## 15.2 移动端

```text
单列布局
导航栏高度 56px
按钮可以占满宽度
卡片圆角略小
减少背景光斑数量
降低 blur 值以提升性能
```

移动端推荐：

```css
@media (max-width: 768px) {
  .glass-card {
    border-radius: 20px;
    backdrop-filter: blur(16px) saturate(130%);
    -webkit-backdrop-filter: blur(16px) saturate(130%);
  }
}
```

---

## 16. 深色模式规范

深色模式不是简单反色。

需要做到：

```text
背景更深
卡片更克制
边框更弱
主色略微提亮
阴影更明显
文字保持高对比
```

深色背景示例：

```css
[data-theme="dark"] body {
  background:
    radial-gradient(circle at 20% 10%, rgba(104, 216, 255, 0.14), transparent 34%),
    radial-gradient(circle at 80% 20%, rgba(177, 140, 255, 0.16), transparent 32%),
    radial-gradient(circle at 45% 90%, rgba(255, 154, 203, 0.10), transparent 36%),
    var(--bg-base);
}
```

---

## 17. 可访问性规范

必须保证：

- 正文和背景对比度足够
- 按钮有明显 focus 状态
- 表单有错误提示
- 图标按钮必须有 aria-label
- 不依赖颜色表达唯一信息
- 动画可关闭
- 键盘可以完整操作

示例：

```html
<button aria-label="接收一架纸飞机">
  <svg></svg>
</button>
```

---

## 18. 性能规范

毛玻璃效果比较吃性能，移动端尤其明显。

## 18.1 使用限制

```text
同屏 backdrop-filter 元素不超过 8 个
大面积 blur 不超过 2 个
弹窗打开时避免背景继续复杂动画
移动端 blur 建议 12px ~ 18px
桌面端 blur 建议 18px ~ 30px
```

---

## 18.2 降级方案

如果浏览器不支持 `backdrop-filter`：

```css
@supports not ((backdrop-filter: blur(12px)) or (-webkit-backdrop-filter: blur(12px))) {
  .glass-card {
    background: rgba(255, 255, 255, 0.88);
  }

  [data-theme="dark"] .glass-card {
    background: rgba(28, 36, 56, 0.92);
  }
}
```

---

## 19. 页面专项规范

## 19.1 首页

结构：

```text
顶部导航
主视觉区
投递按钮
接收按钮
随机纸飞机预览
网站统计
```

要求：

```text
背景有柔和光斑
主标题突出
按钮有漂浮感
纸飞机插画可轻微上下浮动
```

---

## 19.2 投递页

结构：

```text
页面标题
纸飞机颜色选择
昵称输入框
内容文本域
字数统计
投递按钮
说明文案
```

重点：

```text
写内容区域必须最清晰
投递按钮放在视觉焦点位置
投递成功后播放纸飞机飞走动画
```

---

## 19.3 接收页

结构：

```text
接收按钮
纸飞机展示卡片
内容正文
操作按钮
再接收一架
```

重点：

```text
接收前保持神秘感
接收后卡片缓慢展开
正文区域不能太透明
```

---

## 19.4 收藏页

结构：

```text
收藏列表
搜索 / 筛选，可选
纸飞机卡片
取消收藏按钮
```

要求：

```text
列表卡片密度不要太高
每张卡片最多显示 3 ~ 5 行正文
长内容进入详情页查看
```

---

## 19.5 管理后台

后台不需要太梦幻。

推荐：

```text
保留云母底色
减少毛玻璃强度
表格使用清晰实体背景
操作按钮状态明确
危险操作使用红色
```

---

## 20. 禁止事项

不要这样做：

```text
全页面到处都是透明玻璃
文字直接压在复杂背景图上
背景动画太花
blur 值过大导致卡顿
按钮没有边界
深色模式只做反色
阴影又黑又硬
圆角大小混乱
玻璃卡片没有边框
弹窗和背景层级不明显
```

---

## 21. 完整 CSS 基础模板

```css
:root {
  --color-primary: #6C8CFF;
  --color-primary-hover: #5D7CF0;
  --color-primary-active: #4D6CE0;
  --color-primary-soft: rgba(108, 140, 255, 0.16);

  --bg-base: #EEF3FF;
  --bg-mica: rgba(245, 248, 255, 0.72);
  --bg-glass: rgba(255, 255, 255, 0.48);
  --bg-glass-strong: rgba(255, 255, 255, 0.72);

  --text-primary: #172033;
  --text-secondary: rgba(23, 32, 51, 0.72);
  --text-muted: rgba(23, 32, 51, 0.48);

  --border-glass: rgba(255, 255, 255, 0.58);
  --border-soft: rgba(80, 100, 140, 0.14);

  --radius-sm: 12px;
  --radius-md: 16px;
  --radius-lg: 24px;
  --radius-xl: 32px;

  --shadow-sm: 0 6px 18px rgba(31, 45, 80, 0.10);
  --shadow-md: 0 16px 40px rgba(31, 45, 80, 0.14);
  --shadow-lg: 0 24px 70px rgba(31, 45, 80, 0.20);
}

[data-theme="dark"] {
  --bg-base: #0E1320;
  --bg-mica: rgba(18, 24, 38, 0.74);
  --bg-glass: rgba(28, 36, 56, 0.50);
  --bg-glass-strong: rgba(34, 44, 68, 0.76);

  --text-primary: #F2F6FF;
  --text-secondary: rgba(242, 246, 255, 0.72);
  --text-muted: rgba(242, 246, 255, 0.46);

  --border-glass: rgba(255, 255, 255, 0.16);
  --border-soft: rgba(255, 255, 255, 0.08);

  --shadow-sm: 0 6px 18px rgba(0, 0, 0, 0.22);
  --shadow-md: 0 16px 40px rgba(0, 0, 0, 0.32);
  --shadow-lg: 0 24px 70px rgba(0, 0, 0, 0.44);
}

body {
  min-height: 100vh;
  margin: 0;
  color: var(--text-primary);
  font-family:
    "Alibaba PuHuiTi",
    "Source Han Sans SC",
    "HarmonyOS Sans SC",
    "Microsoft YaHei",
    system-ui,
    sans-serif;
  background:
    radial-gradient(circle at 20% 10%, rgba(104, 216, 255, 0.32), transparent 32%),
    radial-gradient(circle at 80% 20%, rgba(177, 140, 255, 0.28), transparent 30%),
    radial-gradient(circle at 45% 90%, rgba(255, 154, 203, 0.18), transparent 34%),
    var(--bg-base);
}

.glass-card {
  position: relative;
  background: var(--bg-glass);
  backdrop-filter: blur(22px) saturate(140%);
  -webkit-backdrop-filter: blur(22px) saturate(140%);
  border: 1px solid var(--border-glass);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-md);
}

.button-primary {
  height: 44px;
  padding: 0 22px;
  border: 1px solid rgba(255,255,255,0.42);
  border-radius: 999px;
  background: linear-gradient(135deg, #6C8CFF, #B18CFF);
  color: white;
  font-weight: 600;
  cursor: pointer;
  box-shadow: 0 10px 24px rgba(108, 140, 255, 0.28);
  transition:
    transform 180ms ease-out,
    box-shadow 180ms ease-out,
    opacity 180ms ease-out;
}

.button-primary:hover {
  transform: translateY(-1px);
  box-shadow: 0 14px 32px rgba(108, 140, 255, 0.34);
}

.button-primary:active {
  transform: translateY(0) scale(0.98);
}
```

---

## 22. 参考资料

- Microsoft Learn：Mica material  
  https://learn.microsoft.com/windows/apps/design/style/mica
- Microsoft Learn：Acrylic material  
  https://learn.microsoft.com/windows/apps/design/style/acrylic
- Microsoft Fluent Design System  
  https://fluent2.microsoft.design/

---

## 23. 总结

本项目的毛玻璃云母风格不是“透明越多越好”，而是：

```text
底层柔和
内容清晰
组件漂浮
边界明确
动效轻盈
性能可控
```

最终应该呈现：

> 用户像在云层上展开一架纸飞机，而不是在一堆半透明塑料片里找按钮。
