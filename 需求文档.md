# 《纸机驿站 PaperPlane Relay》需求文档 v0.1

## 1. 项目简介

开发一个匿名随机内容交换网站。用户不能直接与指定对象聊天，而是通过“投递纸飞机”的方式上传一段内容。系统会将这架纸飞机随机发送给其他用户，同时用户也可以随机接收到别人投递的纸飞机。

核心体验：

> 投出一架纸飞机，随机接住一个陌生人的世界。

---

## 2. 项目定位

### 中文名

**纸机驿站**

### 英文名

**PaperPlane Relay**

### 网站类型

匿名随机内容交换 / 轻社交 / 漂流瓶式互动网站

### 目标用户

喜欢匿名表达、随机社交、情绪分享、碎片化交流的用户。

---

## 3. 核心玩法

用户进入网站后，可以：

1. 创建一架“纸飞机”
2. 在纸飞机里写入内容
3. 点击“投递”
4. 系统随机将该纸飞机放入公共池
5. 其他用户可以随机“接收一架纸飞机”
6. 用户看到别人投递的内容
7. 用户可以选择：
   - 收藏
   - 点赞
   - 举报
   - 再接收一架
   - 回复一架新的纸飞机，但不是直接私聊

---

## 4. 功能需求

## 4.1 前端功能

### 首页

首页需要展示：

- 网站名称：纸机驿站
- 简短介绍
- “投递纸飞机”按钮
- “接收纸飞机”按钮
- 当前已投递纸飞机数量
- 当前在线人数，可选

---

### 投递纸飞机页面

用户可以创建一架纸飞机。

需要支持：

- 输入文字内容
- 选择纸飞机颜色或样式
- 添加昵称，可选
- 匿名投递
- 内容字数限制
- 投递按钮
- 投递成功动画

基础字段：

```text
内容：必填
昵称：可选
纸飞机颜色：可选
是否匿名：默认匿名
```

限制建议：

```text
文字长度：1 ~ 500 字
昵称长度：0 ~ 20 字
```

---

### 接收纸飞机页面

用户点击“接收纸飞机”后，系统随机返回一条别人投递的内容。

页面需要展示：

- 纸飞机展开动画
- 内容正文
- 投递时间
- 昵称，如果用户填写了
- 点赞按钮
- 收藏按钮
- 举报按钮
- 再接收一架按钮

注意：

用户不能指定接收某个人的纸飞机。

---

### 我的收藏页面

用户可以查看自己收藏过的纸飞机。

需要支持：

- 收藏列表
- 取消收藏
- 查看纸飞机详情

如果不做登录系统，可以使用浏览器本地存储保存收藏。

---

### 举报页面 / 举报弹窗

用户可以举报违规内容。

举报原因包括：

- 垃圾广告
- 辱骂攻击
- 色情低俗
- 政治敏感
- 违法内容
- 其他原因

---

## 4.2 后端功能

### 纸飞机管理

后端需要支持：

- 创建纸飞机
- 随机获取纸飞机
- 查询纸飞机详情
- 点赞纸飞机
- 举报纸飞机
- 屏蔽违规纸飞机
- 统计纸飞机总数

---

### 随机分发逻辑

用户接收纸飞机时，后端从数据库中随机选取一条可展示内容。

需要避免：

- 用户收到自己刚投递的纸飞机
- 短时间内重复收到同一架纸飞机
- 收到已被封禁的纸飞机
- 收到举报过多的纸飞机

推荐逻辑：

```text
从未封禁、举报数低、不是当前用户创建的纸飞机中随机返回一条。
```

---

### 内容审核

需要基础审核机制：

- 敏感词过滤
- 举报计数
- 超过举报阈值自动隐藏
- 管理员后台人工审核

建议规则：

```text
举报次数 >= 3：自动隐藏
举报次数 >= 5：进入管理员重点审核列表
```

---

### 管理员后台

管理员可以：

- 查看所有纸飞机
- 搜索内容
- 查看举报列表
- 删除违规内容
- 恢复误删内容
- 封禁 IP 或用户
- 查看网站统计数据

---

## 5. 用户系统

MVP 版本可以不做完整登录。

### 方案一：无账号模式

使用浏览器生成匿名 ID。

优点：

- 使用门槛低
- 更符合匿名玩法
- 开发简单

缺点：

- 换设备后数据丢失
- 管理能力较弱

---

### 方案二：账号模式

支持邮箱、手机号或第三方登录。

优点：

- 可以跨设备保存收藏
- 可做用户等级
- 更方便管理违规用户

缺点：

- 开发复杂
- 匿名感降低

---

### 推荐方案

第一版使用：

```text
匿名 ID + 本地存储 + IP 风控
```

后续再增加账号系统。

---

## 6. 数据库设计

### paper_planes 表

```sql
id
content
nickname
color
user_id
ip_hash
like_count
report_count
status
created_at
updated_at
```

字段说明：

```text
id：纸飞机唯一 ID
content：纸飞机内容
nickname：昵称，可为空
color：纸飞机颜色
user_id：匿名用户 ID
ip_hash：加密后的 IP 标识
like_count：点赞数
report_count：举报数
status：状态，normal / hidden / deleted
created_at：创建时间
updated_at：更新时间
```

---

### reports 表

```sql
id
paper_plane_id
user_id
reason
created_at
```

---

### likes 表

```sql
id
paper_plane_id
user_id
created_at
```

---

### favorites 表

```sql
id
paper_plane_id
user_id
created_at
```

---

## 7. API 设计

### 创建纸飞机

```http
POST /api/paper-planes
```

请求：

```json
{
  "content": "今天也想被世界温柔对待。",
  "nickname": "匿名旅人",
  "color": "blue"
}
```

返回：

```json
{
  "success": true,
  "id": "plane_001"
}
```

---

### 随机接收纸飞机

```http
GET /api/paper-planes/random
```

返回：

```json
{
  "id": "plane_001",
  "content": "今天也想被世界温柔对待。",
  "nickname": "匿名旅人",
  "color": "blue",
  "like_count": 12,
  "created_at": "2026-06-06T12:00:00"
}
```

---

### 点赞纸飞机

```http
POST /api/paper-planes/{id}/like
```

---

### 收藏纸飞机

```http
POST /api/paper-planes/{id}/favorite
```

---

### 举报纸飞机

```http
POST /api/paper-planes/{id}/report
```

请求：

```json
{
  "reason": "垃圾广告"
}
```

---

## 8. 页面结构

```text
/
首页

/send
投递纸飞机

/receive
接收纸飞机

/favorites
我的收藏

/about
关于网站

/admin
管理员后台
```

---

## 9. 前端技术建议

推荐技术栈：

```text
Vue 3 / React
TypeScript
Tailwind CSS
Axios / Fetch
Vite
```

前端重点：

- 纸飞机动画
- 页面过渡动画
- 响应式布局
- 移动端适配
- 深色模式
- 防止重复提交

---

## 10. 后端技术建议

推荐技术栈：

```text
Node.js + Express / NestJS
PostgreSQL / MySQL
Redis
JWT 可选
Prisma ORM
```

后端重点：

- 随机分发算法
- 内容审核
- 举报系统
- 频率限制
- IP 风控
- 数据统计

---

## 11. 安全与风控

需要限制：

- 单个用户一分钟内最多投递次数
- 单个 IP 每日最多投递次数
- 内容长度
- 敏感词
- 重复内容
- 恶意刷赞
- 恶意举报

建议限制：

```text
每分钟最多投递 3 次
每天最多投递 50 次
每分钟最多接收 20 次
同一用户不能重复点赞同一纸飞机
```

---

## 12. MVP 第一版功能

第一版只需要做这些：

- 首页
- 投递纸飞机
- 随机接收纸飞机
- 点赞
- 举报
- 简单后台管理
- 基础内容过滤
- 匿名用户 ID

暂时不做：

- 私聊
- 关注
- 评论区
- 好友系统
- 复杂账号系统
- 语音和图片上传

---

## 13. 后续扩展功能

以后可以加入：

- 图片纸飞机
- 语音纸飞机
- 定时投递
- 限定主题池
- 情绪标签
- 同城纸飞机
- 每日精选
- 纸飞机地图
- 成就系统
- 用户等级
- AI 内容审核
- AI 生成纸飞机封面

---

## 14. 核心规则总结

```text
用户不能直接聊天。
用户只能投递纸飞机。
用户只能随机接收纸飞机。
回复也必须以新纸飞机的形式进入随机池。
系统不建立固定双向关系。
```

这能保证网站的核心感觉是：

> 不是聊天软件，而是一个陌生人之间的随机消息交换站。
